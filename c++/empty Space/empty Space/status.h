#pragma once
#include <string>
#include <vector>

//Adds a status to the vector
void addStatus(std::string display, std::vector<std::string>* status);
//Clears everything in the status box
void clearStatus(std::vector<std::string>* status);
